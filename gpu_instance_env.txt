# Update your GPU instance .env file with these settings:

# Disable GPU bridge since we're running locally now
GPU_ACCESS_ENABLED=false
PRIME_ENABLED=false

# Use local Ollama
OLLAMA_BASE_URL=http://olloma:11434
DEFAULT_MODEL=llama3.1:8b

# Keep other settings as needed
LOG_LEVEL=INFO
MAX_CONCURRENT_AGENTS=3
AGENT_TIMEOUT=300